node_modules/
.next/
.env
.env.local
.env.production
.env.development
*.log

project_dump.txt

# Copied from node_modules/pdfjs-dist at postinstall time (see
# scripts/copy-pdf-worker.js) — always regenerated to match whatever
# pdfjs-dist version is actually installed, never hand-edited or committed.
public/pdf.worker.min.mjs

.vercel
.env*

# AI Chat / Atomic Guru Google Sheets sync credential — never commit
service-account.json
